import { Btn } from './Btn';

export const Banner = () => {
    return (
        <div className="banner">
            <p>We always solve client's problem in the most optimal and effective way.</p>
            <Btn dark={true}/>
        </div>
    )
}